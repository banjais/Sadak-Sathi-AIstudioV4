import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>Sadak-Sathi Minimal Working Page</h1>;

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
